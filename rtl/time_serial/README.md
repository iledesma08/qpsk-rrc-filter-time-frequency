# rtl/time_serial — time-domain filter, serial version

Run the placeholder smoke test from the repository root:

```bash
bash rtl/time_serial/run.sh
```

The actual one-MAC-per-sample time-domain filter is T30; the current module
is only a compileable shell for the repository toolchain.
