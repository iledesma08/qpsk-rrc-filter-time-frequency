# rtl/freq_opt — frequency-domain filter, optimized version

Initial proposal: unfolded (plus folded as contrast if area allows) (T42–T43, changeable via ADR).
Must pass vector matching + timing closure + PPA report.

Run the placeholder smoke test from the repository root:

```bash
bash rtl/freq_opt/run.sh
```
